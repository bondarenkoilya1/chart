import { type ChartConfig } from "@/components/ui/chart.js";

export const chartConfig = {
  desktop: {
    label: "Desktop",
    color: "#25b6eb"
  },
  mobile: {
    label: "Mobile",
    color: "#60ccfa"
  }
} satisfies ChartConfig;
